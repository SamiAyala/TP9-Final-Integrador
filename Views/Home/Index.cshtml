@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    @foreach (Board item in ViewBag.ListBoard)
    {
        <button type="button" href="Board">@item.Nombre</button>
    }
</div>
