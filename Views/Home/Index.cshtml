@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    @foreach (Board item in ViewBag.ListBoard)
    {
        <a href='@Url.Action("CargarBoard","Home",new {item=@item})'>@item.Nombre</a>
    }
</div>
